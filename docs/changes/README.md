# Change Records

Every meaningful change should add a change record (copy `TEMPLATE.md`).

Goals:
- Prevent feature loss (CAP-12)
- Make UI->logic wiring explicit
- Make verification steps repeatable
